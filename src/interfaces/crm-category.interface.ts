// the CRM Category (of company) interface

export default interface CRMCompanyCategoryInterface {
  createdAt: string | Date; // IOS Date
  updateAt: string | Date; // IOS Date
  id: string;
  // name of the category
  name: string;
}
