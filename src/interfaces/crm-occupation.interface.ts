// the CRM Occupation (of contact) interface

export default interface CRMContactOccupationInterface {
  createdAt: string | Date; // IOS Date
  updateAt: string | Date; // IOS Date
  id: string;
  // name of the occupation
  name: string;
}
