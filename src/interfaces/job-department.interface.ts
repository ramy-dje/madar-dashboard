// the job department interface
export default interface JobDepartmentInterface {
  name: string;
  createdAt: string | Date;
  id: string;
}
// the create job department interface
export interface CreateJobDepartmentInterface {
  name: string;
}
